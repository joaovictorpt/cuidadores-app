// Central place for the site's name/tagline so they're never hardcoded
// separately in app/layout.tsx, app/page.tsx, or anywhere else that needs
// them later (emails, other marketing pages, etc.).
export const siteConfig = {
  name: "Trevo",
  tagline: "Cuidado que conecta",
};
